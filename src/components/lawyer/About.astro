---

---
<section class="about" id="nosotros">
    <div class="container">
        <div class="about-content">
            <!-- La imagen ahora es el ancla visual -->
            <div class="about-image">
                <img src="https://images.pexels.com/photos/4427507/pexels-photo-4427507.jpeg?_gl=1*13vbz5b*_ga*MjExMDI1NTg5MC4xNzU1Njk0OTM5*_ga_8JE65Q40S6*czE3NTU2OTQ5MzkkbzEkZzEkdDE3NTU2OTYzNzgkajU2JGwwJGgw" alt="Equipo de abogados en una reunión profesional">
            </div>
            <!-- El texto se superpone como una tarjeta -->
            <div class="about-text-card">
                <p class="pre-title">NUESTRO COMPROMISO</p>
                <h2>25 Años de Excelencia Jurídica</h2>
                <p class="subtitle">Somos un bufete con más de dos décadas de experiencia. Nuestro equipo multidisciplinario trabaja con pasión para ofrecer un servicio personalizado, transparente y eficaz.</p>
                <div class="stats">
                    <div class="stat-item">
                        <span class="stat-number">1,500+</span>
                        <span class="stat-label">Casos Ganados</span>
                    </div>
                    <div class="stat-item">
                        <span class="stat-number">25</span>
                        <span class="stat-label">Años Activos</span>
                    </div>
                    <div class="stat-item">
                        <span class="stat-number">95%</span>
                        <span class="stat-label">Tasa de Éxito</span>
                    </div>
                    <div class="stat-item">
                        <span class="stat-number">24/7</span>
                        <span class="stat-label">Atención</span>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    .about {
        padding: 8rem 2rem;
        background: linear-gradient(180deg, #0a0a0b 20%, #1e293b 100%);
        position: relative;
        overflow: hidden;
    }

    .container {
        max-width: 1100px;
        margin: 0 auto;
    }

    .about-content {
        display: flex;
        align-items: center;
        position: relative;
    }

    /* --- La Imagen como Base del Diseño --- */
    .about-image {
        flex: 0 0 65%; /* La imagen ocupa el 65% del espacio */
        position: relative;
        z-index: 1;
    }

    .about-image img {
        width: 100%;
        height: auto;
        border-radius: 16px;
        box-shadow: 0 25px 50px -12px rgba(0,0,0,0.5);
        display: block;
    }

    /* --- La Tarjeta de Texto Superpuesta --- */
    .about-text-card {
        flex: 0 0 50%; /* El texto ocupa el 50% */
        position: relative;
        z-index: 2;
        /* El margen negativo crea la superposición */
        margin-left: -15%; 
        background-color: #1e293b;
        padding: 3rem;
        border-radius: 16px;
        box-shadow: 0 20px 40px rgba(0,0,0,0.4);
        border: 1px solid rgba(148, 163, 184, 0.2);
        backdrop-filter: blur(10px); /* Efecto cristalino sutil */
    }

    .about-text-card .pre-title {
        font-size: 0.8rem;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 1.5px;
        color: #f1c40f;
        margin-bottom: 1rem;
    }

    .about-text-card h2 {
        font-size: 2.5rem;
        margin-bottom: 1.5rem;
        color: #fff;
        line-height: 1.2;
    }

    .about-text-card .subtitle {
        font-size: 1.05rem;
        line-height: 1.7;
        margin-bottom: 2.5rem;
        color: #94a3b8;
    }

    /* --- Estadísticas en una sola fila --- */
    .stats {
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        gap: 1.5rem;
        border-top: 1px solid rgba(148, 163, 184, 0.2);
        padding-top: 2rem;
    }

    .stat-item {
        text-align: center;
    }

    .stat-number {
        font-size: 2.2rem;
        font-weight: bold;
        color: #f1c40f;
        display: block;
        line-height: 1;
    }

    .stat-label {
        font-size: 0.8rem;
        color: #94a3b8;
        margin-top: 0.5rem;
    }

    /* --- Diseño Responsivo para Móviles --- */
    @media (max-width: 992px) {
        .about {
            padding: 5rem 1.5rem;
        }
        .about-content {
            flex-direction: column; /* Apila los elementos */
        }
        .about-image {
            width: 100%;
            margin-bottom: 2rem;
        }
        .about-text-card {
            margin-left: 0; /* Elimina la superposición */
            width: 100%;
            padding: 2rem;
        }
        .about-text-card h2 {
            font-size: 2rem;
        }
        .stats {
            /* Vuelve a 2x2 en móviles para no ser tan pequeño */
            grid-template-columns: repeat(2, 1fr);
        }
    }
</style>