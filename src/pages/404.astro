---
const { pathname } = Astro.url;

---
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

</head>
<div class="page-404">
  <div class="bg-animated">
    <div class="bubble bubble-a" style="animation-delay: 0s;"></div>
    <div class="bubble bubble-b" style="animation-delay: 1s;"></div>
    <div class="bubble bubble-c" style="animation-delay: 2s;"></div>
  </div>

  <div class="content">
    <div class="fade-in">
     

      <!-- Código de Error -->
      <h1 class="error-code">404</h1>

      <!-- Mensaje de Error -->
      <div class="fade-in" style="animation-delay: 300ms;">
        <h2 class="title">Página no encontrada</h2>
        <p class="subtitle">
          Lo sentimos, la página que buscas no existe o ha sido movida.
          Verifica la URL o regresa al inicio para continuar navegando.
        </p>
      </div>

      <!-- Ruta Actual -->
      {pathname !== "/" && (
        <div class="fade-in" style="animation-delay: 500ms;">
          <div class="route-chip">
            <p class="route-text">
              Ruta solicitada: <span class="route-path">{pathname}</span>
            </p>
          </div>
        </div>
      )}

      <!-- Botones de Acción -->
      <div class="actions fade-in" style="animation-delay: 700ms;">
        <a href="/" class="btn btn-primary">
          <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="icon">
            <path d="m3 9 9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"></path>
            <polyline points="9 22 9 12 15 12 15 22"></polyline>
          </svg>
          Ir al Inicio
        </a>

        <button id="back-button" class="btn btn-secondary">
          <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="icon">
            <path d="m12 19-7-7 7-7"></path><path d="M19 12H5"></path>
          </svg>
          Regresar
        </button>
      </div>

      <!-- Ayuda Adicional -->
    </div>
  </div>
</div>

<script>
  document.getElementById('back-button')?.addEventListener('click', () => {
    window.history.back();
  });
</script>
<style >
    html,body{
        padding: 0;
        margin: 0;
    }
    /* Variables de tema (tuyas) */
    :root {
    --color-dark: #0d1117;
    --color-primary: #161b22;
    --color-secondary: #010409;
    --color-accent: #30c59b;
    --color-accent-hover: #27a07e;
    --color-text: #c9d1d9;
    --color-text-muted: #8b949e;
    --font-body: "Inter", system-ui, sans-serif;
    --font-heading: "Poppins", system-ui, sans-serif;
    --color-border: #334155;
    }

    /* Página y fondo */
    .page-404 {
    min-height: 90vh;
    background-image: linear-gradient(160deg, var(--color-secondary) 0%, var(--color-dark) 100%);
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 1rem;
    position: relative;
    overflow: hidden;
    color: var(--color-text);
    font-family: var(--font-body);
    }

    /* Fondo animado (burbujas) */
    .bg-animated {
    position: absolute;
    inset: 0;
    overflow: hidden;
    }
    .bubble {
    position: absolute;
    border-radius: 9999px;
    animation: float 6s ease-in-out infinite;
    }
    .bubble-a { top: 25%; left: 25%; width: 8rem; height: 8rem; background-color: color-mix(in oklab, var(--color-accent) 10%, transparent); }
    .bubble-b { top: 75%; right: 25%; width: 6rem; height: 6rem; background-color: color-mix(in oklab, var(--color-accent) 18%, transparent); }
    .bubble-c { top: 50%; left: 75%; width: 4rem; height: 4rem; background-color: color-mix(in oklab, var(--color-accent) 10%, transparent); }

    /* Fallback si tu navegador no soporta color-mix */
    @supports not (color: color-mix(in oklab, white, black)) {
    .bubble-a { background-color: rgba(48, 197, 155, 0.10); }
    .bubble-b { background-color: rgba(48, 197, 155, 0.18); }
    .bubble-c { background-color: rgba(48, 197, 155, 0.10); }
    }

    /* Contenido centrado */
    .content {
    position: relative;
    z-index: 10;
    max-width: 56rem;
    width: 100%;
    margin: 0 auto;
    text-align: center;
    }

    /* Ilustración */
    .art {
    position: relative;
    margin-bottom: 2rem;
    }
    .art-glow {
    position: absolute;
    inset: 0;
    background-image: linear-gradient(90deg, color-mix(in oklab, var(--color-accent) 85%, white), var(--color-accent));
    opacity: 0.10;
    filter: blur(64px);
    }
    @supports not (color: color-mix(in oklab, white, black)) {
    .art-glow {
        background-image: linear-gradient(90deg, #5be0bf, var(--color-accent));
    }
    }

    /* Titulares */
    .error-code {
    font-family: var(--font-heading);
    font-weight: 700;
    font-size: 6rem;
    line-height: 1;
    margin: 0 0 1rem 0;
    background-image: linear-gradient(90deg, color-mix(in oklab, var(--color-accent) 85%, white), var(--color-accent));
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
    animation: pulse-glow 3s ease-in-out infinite;
    }
    @supports not (color: color-mix(in oklab, white, black)) {
    .error-code {
        background-image: linear-gradient(90deg, #5be0bf, var(--color-accent));
    }
    }
    @media (min-width: 768px) {
    .error-code { font-size: 8rem; }
    }

    .title {
    font-family: var(--font-heading);
    font-weight: 600;
    font-size: 1.875rem;
    line-height: 2.25rem;
    color: var(--color-text);
    margin: 0 0 1rem 0;
    }
    @media (min-width: 768px) {
    .title { font-size: 2.25rem; line-height: 2.5rem; }
    }

    .subtitle {
    font-size: 1.25rem;
    line-height: 1.75rem;
    color: var(--color-text-muted);
    margin: 0 0 2rem 0;
    max-width: 42rem;
    margin-left: auto;
    margin-right: auto;
    line-height: 1.625;
    }

    /* Chips de ruta */
    .route-chip {
    display: inline-block;
    background-color: color-mix(in oklab, var(--color-primary) 50%, transparent);
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    border: 1px solid var(--color-border);
    border-radius: 0.5rem;
    padding: 1rem;
    margin-bottom: 2rem;
    }
    @supports not (color: color-mix(in oklab, white, black)) {
    .route-chip { background-color: rgba(22, 27, 34, 0.5); }
    }
    .route-text {
    margin: 0;
    font-size: 0.875rem;
    line-height: 1.25rem;
    color: var(--color-text-muted);
    }
    .route-path {
    color: var(--color-accent);
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
    }

    /* Botones */
    .actions {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    justify-content: center;
    align-items: center;
    }
    @media (min-width: 640px) {
    .actions { flex-direction: row; }
    }

    .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    white-space: nowrap;
    border-radius: 0.375rem;
    font-size: 0.875rem;
    font-weight: 500;
    height: 2.75rem;
    padding: 0 2rem;
    border: 1px solid transparent;
    text-decoration: none;
    transition: color 300ms cubic-bezier(0.4,0,0.2,1),
                background-color 300ms cubic-bezier(0.4,0,0.2,1),
                border-color 300ms cubic-bezier(0.4,0,0.2,1),
                box-shadow 0.2s ease,
                transform 0.2s cubic-bezier(0.34, 1.56, 0.64, 1);
    color: var(--color-text);
    }
    .btn .icon {
    margin-right: 0.5rem;
    width: 1.25rem; height: 1.25rem;
    }

    /* Primario (gradiente en clave verde-acento) */
    .btn-primary {
    background-image: linear-gradient(
        90deg,
        color-mix(in oklab, var(--color-accent) 86%, black),
        var(--color-accent)
    );
    color: var(--color-secondary);
    border-color: color-mix(in oklab, var(--color-border) 20%, transparent);
    box-shadow: 0 0 20px 0px color-mix(in oklab, var(--color-accent) 30%, transparent);
    }
    @supports not (color: color-mix(in oklab, white, black)) {
    .btn-primary {
        background-image: linear-gradient(90deg, #29b88e, var(--color-accent));
        border-color: rgba(51, 65, 85, 0.2);
        box-shadow: 0 0 20px 0px rgba(48, 197, 155, 0.3);
    }
    }
    .btn-primary:hover {
    box-shadow: 0 0 30px 5px color-mix(in oklab, var(--color-accent) 40%, transparent);
    transform: scale(1.05);
    }

    /* Secundario (vidrioso sobre el fondo) */
    .btn-secondary {
    background-color: color-mix(in oklab, var(--color-secondary) 50%, transparent);
    color: var(--color-text);
    border: 1px solid var(--color-border);
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    }
    @supports not (color: color-mix(in oklab, white, black)) {
    .btn-secondary { background-color: rgba(1, 4, 9, 0.5); }
    }
    .btn-secondary:hover {
    background-color: color-mix(in oklab, var(--color-accent) 12%, transparent);
    color: var(--color-accent);
    border-color: var(--color-accent);
    }

    /* Ghost */
    .btn-ghost {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    white-space: nowrap;
    border-radius: 0.375rem;
    font-size: 0.875rem;
    font-weight: 500;
    height: 2.25rem;
    padding: 0 0.75rem;
    color: var(--color-text-muted);
    background: transparent;
    border: 1px solid transparent;
    transition: color 300ms cubic-bezier(0.4,0,0.2,1), background-color 300ms cubic-bezier(0.4,0,0.2,1), border-color 300ms cubic-bezier(0.4,0,0.2,1);
    }
    .btn-ghost .icon.sm { width: 1rem; height: 1rem; margin-right: 0.5rem; }
    .btn-ghost:hover {
    color: var(--color-accent);
    background-color: color-mix(in oklab, var(--color-accent) 12%, transparent);
    }
    @supports not (color: color-mix(in oklab, white, black)) {
    .btn-ghost:hover { background-color: rgba(48, 197, 155, 0.12); }
    }

    /* Accesibilidad / estados */
    .btn:focus-visible { outline: none; box-shadow: 0 0 0 2px var(--color-accent); }
    .btn:disabled { pointer-events: none; opacity: 0.5; }

    /* Sección de ayuda */
    .help { margin-top: 3rem; }
    .help-text {
    margin: 0 0 1rem 0;
    font-size: 0.875rem;
    color: var(--color-text-muted);
    }
    .help-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    justify-content: center;
    }

    /* Animaciones */
    @keyframes float {
    0%, 100% { transform: translateY(0px); }
    50% { transform: translateY(-20px); }
    }
    @keyframes fade-in-up {
    0% { opacity: 0; transform: translateY(20px); }
    100% { opacity: 1; transform: translateY(0); }
    }
    @keyframes pulse-glow {
    0%, 100% { opacity: 1; text-shadow: 0 0 10px var(--color-accent); }
    50% { opacity: 0.8; text-shadow: 0 0 20px var(--color-accent); }
    }
    .fade-in { animation: fade-in-up 1s ease-out forwards; }

    /* Iconos inline */
    .icon { flex: 0 0 auto; }
    </style>